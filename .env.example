# Rediacc CLI Environment Configuration
# Copy this file to .env and edit with your API settings

# Required API configuration
SYSTEM_HTTP_PORT=443
SYSTEM_API_URL=https://sandbox.rediacc.com

# Optional admin credentials (used by CLI login command)
# Replace with your actual admin credentials
SYSTEM_ADMIN_EMAIL=admin@yourorganization.com
SYSTEM_ADMIN_PASSWORD=your_secure_password

# Test activation code (default: 111111 in test environment)
REDIACC_TEST_ACTIVATION_CODE=111111

# Optional debugging (enables verbose output in workflow operations)
REDIACC_VERBOSE=false

# Optional config directory override (mainly for Docker containers)
# REDIACC_CONFIG_DIR=/path/to/config/directory

# MSYS2 installation path (required for sync operations on Windows)
# Set this to your MSYS2 installation directory if not in default location
REDIACC_MSYS2_ROOT=C:\msys64

# System detection (automatically set by MSYS2 environment)
# MSYSTEM=MINGW64
